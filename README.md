# VSCucumberAutoComplete-Examples

To test some type of steps:
1. Clone this repo or just download ZIP 
2. Open folder you would like to test in the VSCode (ex. open "VSCucumberAutoComplete-Examples/js")